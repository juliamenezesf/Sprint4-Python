Integrantes:
RM565568 - Júlia Menezes
RM559932 = Pedro Costa


# 🧩 1️⃣ Preparar o ambiente

Abra o PowerShell na pasta do projeto (onde está `requirements.txt`).

### Ativar ambiente virtual

```powershell
py -3.11 -m venv .venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1

```

### Instalar dependências

```powershell
pip install -r requirements.txt

```

### Definir variáveis de ambiente

Ajuste o usuário e senha do seu banco FIAP:

```powershell
$env:DATABASE_URL = "oracle+oracledb://USUARIO:SENHA@oracle.fiap.com.br:1521/ORCL"
$env:FRONTEND_ORIGIN = "http://localhost:5173,http://127.0.0.1:5173"

```

---

## 🧱 2️⃣ Criar a tabela FAQ no banco Oracle

### Manual (no SQL Developer)

```sql
CREATE TABLE FAQS (
  ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  PERGUNTA VARCHAR2(500) NOT NULL,
  RESPOSTA VARCHAR2(2000) NOT NULL
);

```

Verifique:

```sql
SELECT * FROM FAQS;

```

---

## 🌐 3️⃣ Rodar a API Flask

Execute:

```powershell
python -m app.faq_api

```

Deve aparecer:

```
Running on http://127.0.0.1:5000

```

---

## 🧪 4️⃣ Testar a API com Invoke-RestMethod

### 🩺 Verificar se está online

Com a API rodando em um terminal, abra outro terminal Powershell e execute:

```powershell
Invoke-RestMethod -Uri http://127.0.0.1:5000/

```

---

### 📋 Listar FAQs

```powershell
Invoke-RestMethod -Uri http://127.0.0.1:5000/api/faqs

```

---

### ➕ Criar uma FAQ

```powershell
$body = @{ pergunta = "Como agendar?"; resposta = "Pelo portal do paciente." } | ConvertTo-Json
Invoke-RestMethod -Method Post -Uri http://127.0.0.1:5000/api/faqs -ContentType "application/json" -Body $body

```

---

### ✏️ Atualizar uma FAQ

```powershell
$body = @{ id = 1; pergunta = "Como remarcar?"; resposta = "Na área Minhas Consultas." } | ConvertTo-Json
Invoke-RestMethod -Method Put -Uri http://127.0.0.1:5000/api/faqs -ContentType "application/json" -Body $body

```

---

### ❌ Remover uma FAQ

```powershell
Invoke-RestMethod -Method Delete -Uri http://127.0.0.1:5000/api/faqs/1

```

---

## 🖥️ 5️⃣ Usar o Console FAQ (modo texto)

Com a API rodando em um terminal, abra outro terminal Powershell e execute:

$env:DATABASE_URL = "oracle+oracledb://USUARIO:SENHA@oracle.fiap.com.br:1521/ORCL"
$env:FRONTEND_ORIGIN = "http://localhost:5173,http://127.0.0.1:5173"

```powershell
python -m app.console_faq

```

Menu:

```
1. Listar FAQs
2. Criar FAQ
3. Atualizar FAQ
4. Remover FAQ
0. Sair

```

Depois confirme no SQL Developer:

```sql
SELECT * FROM FAQS ORDER BY ID;

```

---

